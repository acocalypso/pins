name: Build Linux ARM64 Package

on:
  workflow_dispatch:

env:
  BUILD_CONFIGURATION: Release
  TARGET_RUNTIME: linux-arm64
  INSTALL_DIRECTORY: /home/pi/pins
  PUBLISH_DIRECTORY: artifacts/publish
  PACKAGE_ROOT: artifacts/package
  PACKAGE_NAME: pins-linux-arm64
  DEB_ROOT: artifacts/debroot
  DEB_PACKAGE: pins
  DEB_ARCH: arm64
  PLUGINS_INSTALL_BASE: /home/pi/.local/share/NINA/Plugins/3.0.0
  PLUGIN_DEB_BASE: artifacts/plugin-debroot
  PLUGIN_ARTIFACT_DIR: artifacts/plugins

jobs:
  build-linux-arm64:
    name: Build Raspberry Pi package
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: nschloe/action-cached-lfs-checkout@v1
        with:
          submodules: recursive

      - name: Setup .NET 10 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore linux-arm64 dependencies
        run: |
          dotnet restore NINA/NINA.csproj -r ${{ env.TARGET_RUNTIME }}
          dotnet restore System.Windows.Compat/System.Windows.Compat.csproj

      - name: Build System.Windows.Compat
        run: dotnet build System.Windows.Compat/System.Windows.Compat.csproj -c ${{ env.BUILD_CONFIGURATION }} --no-restore

      - name: Build linux-arm64 binaries
        run: dotnet build NINA/NINA.csproj -c ${{ env.BUILD_CONFIGURATION }} -r ${{ env.TARGET_RUNTIME }} --no-restore

      - name: Publish linux-arm64 payload
        run: dotnet publish NINA/NINA.csproj -c ${{ env.BUILD_CONFIGURATION }} -r ${{ env.TARGET_RUNTIME }} --no-build -o ${{ env.PUBLISH_DIRECTORY }}/${{ env.TARGET_RUNTIME }}

      - name: Determine package version
        id: version
        run: |
          version=$(grep -m1 'AssemblyInformationalVersion' CommonAssemblyInfo.cs | sed -E 's/.*"([^"]+)".*/\1/')
          if [ -z "$version" ]; then
            echo "Failed to determine version from CommonAssemblyInfo.cs" >&2
            exit 1
          fi
          echo "Package version: $version"
          echo "package_version=$version" >> "$GITHUB_OUTPUT"

      - name: Stage /home/pi/pins layout
        run: |
          publish_root="${{ env.PUBLISH_DIRECTORY }}/${{ env.TARGET_RUNTIME }}"
          package_root="${{ env.PACKAGE_ROOT }}"
          rm -rf "$package_root"
          mkdir -p "$package_root/pins"
          rsync -a "$publish_root/" "$package_root/pins/"
          echo "${{ env.INSTALL_DIRECTORY }}" > "$package_root/pins/.install_path"

      - name: Replace System.Windows.dll
        run: |
          compat_dll="System.Windows.Compat/bin/${{ env.BUILD_CONFIGURATION }}/net10.0/System.Windows.dll"
          target_dir="${{ env.PACKAGE_ROOT }}/pins"
          if [ ! -f "$compat_dll" ]; then
            echo "Compat build missing at $compat_dll" >&2
            exit 1
          fi
          if [ ! -d "$target_dir" ]; then
            echo "Target directory $target_dir not found" >&2
            exit 1
          fi
          cp -f "$compat_dll" "$target_dir/System.Windows.dll"

      - name: Prepare Debian filesystem
        run: |
          deb_root="${{ env.DEB_ROOT }}"
          install_root="$deb_root/home/pi/pins"
          rm -rf "$deb_root"
          mkdir -p "$install_root"
          rsync -a "${{ env.PACKAGE_ROOT }}/pins/" "$install_root/"

      - name: Add systemd service
        run: |
          deb_root="${{ env.DEB_ROOT }}"
          service_path="$deb_root/etc/systemd/system/pins.service"
          service_dir="$(dirname "$service_path")"
          mkdir -p "$service_dir"
          cp packaging/systemd/pins.service "$service_path"
          chmod 644 "$service_path"

      - name: Add Debian maintainer scripts
        run: |
          deb_root="${{ env.DEB_ROOT }}"
          control_dir="$deb_root/DEBIAN"
          mkdir -p "$control_dir"
          cp packaging/debian/postinst "$control_dir/postinst"
          cp packaging/debian/prerm "$control_dir/prerm"
          chmod 755 "$control_dir/postinst" "$control_dir/prerm"

      - name: Build Debian package
        run: |
          deb_root="${{ env.DEB_ROOT }}"
          control_dir="$deb_root/DEBIAN"
          mkdir -p "$control_dir"
          installed_size=$(du -sk "$deb_root/home/pi/pins" | cut -f1)
          {
            echo "Package: ${{ env.DEB_PACKAGE }}"
            echo "Version: ${{ steps.version.outputs.package_version }}"
            echo "Section: misc"
            echo "Priority: optional"
            echo "Architecture: ${{ env.DEB_ARCH }}"
            echo "Maintainer: N.I.N.A. Team"
            echo "Installed-Size: $installed_size"
            echo "Description: Pins build for Raspberry Pi"
          } > "$control_dir/control"
          dpkg-deb --build "$deb_root" "artifacts/${{ env.DEB_PACKAGE }}_${{ steps.version.outputs.package_version }}_${{ env.DEB_ARCH }}.deb"

      - name: Create install archive
        run: |
          mkdir -p artifacts
          tar -czf "artifacts/${{ env.PACKAGE_NAME }}.tar.gz" -C "${{ env.PACKAGE_ROOT }}" pins

      - name: Generate SHA256 checksums
        run: |
          sha256sum "artifacts/${{ env.PACKAGE_NAME }}.tar.gz" > "artifacts/${{ env.PACKAGE_NAME }}.tar.gz.sha256"
          sha256sum "artifacts/${{ env.DEB_PACKAGE }}_${{ steps.version.outputs.package_version }}_${{ env.DEB_ARCH }}.deb" > "artifacts/${{ env.DEB_PACKAGE }}_${{ steps.version.outputs.package_version }}_${{ env.DEB_ARCH }}.deb.sha256"

      - name: Upload Raspberry Pi package
        uses: actions/upload-artifact@v4
        with:
          name: linux-arm64-package
          path: |
            artifacts/${{ env.PACKAGE_NAME }}.tar.gz
            artifacts/${{ env.PACKAGE_NAME }}.tar.gz.sha256
            artifacts/${{ env.DEB_PACKAGE }}_${{ steps.version.outputs.package_version }}_${{ env.DEB_ARCH }}.deb
            artifacts/${{ env.DEB_PACKAGE }}_${{ steps.version.outputs.package_version }}_${{ env.DEB_ARCH }}.deb.sha256
          retention-days: 30

  plugin-debs:
    name: Build Plugin Debian Packages
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - plugin_id: joko
            install_folder: joko.nina.plugins
            project_path: NINA.Plugins/joko.nina.plugins/Joko.NINA.Plugins/Joko.NINA.Plugins.HocusFocus/Joko.NINA.Plugins.HocusFocus.csproj
            deb_package: pins-plugin-joko
            needs_frontend: false
          - plugin_id: livestack
            install_folder: LiveStack
            project_path: NINA.Plugins/LiveStack/nina.plugin.livestack.csproj
            deb_package: pins-plugin-livestack
            needs_frontend: false
          - plugin_id: ninaapi
            install_folder: ninaAPI
            project_path: NINA.Plugins/ninaAPI/ninaAPI/ninaAPI.csproj
            deb_package: pins-plugin-ninaapi
            needs_frontend: false
          - plugin_id: polaralignment
            install_folder: PolarAlignment
            project_path: NINA.Plugins/PolarAlignment/PolarAlignment/NINA.Plugins.PolarAlignment.csproj
            deb_package: pins-plugin-polaralignment
            needs_frontend: false
          - plugin_id: touchnstars
            install_folder: Touch-N-Stars
            project_path: NINA.Plugins/Touch-N-Stars/Touch-N-Stars/Touch-N-Stars.csproj
            deb_package: pins-plugin-touch-n-stars
            needs_frontend: true
            frontend_repo: https://github.com/Touch-N-Stars/Touch-N-Stars.git
            frontend_branch: pins

    steps:
      - name: Checkout repository
        uses: nschloe/action-cached-lfs-checkout@v1
        with:
          submodules: recursive

      - name: Setup .NET 10 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Disable System.Drawing.Common reference
        shell: bash
        run: |
          set -euo pipefail
          root="${DOTNET_ROOT:-$HOME/.dotnet}"
          ref_dir=$(find "$root/packs/Microsoft.NETCore.App.Ref" -maxdepth 1 -mindepth 1 -type d 2>/dev/null | sort | tail -n1)
          if [ -z "$ref_dir" ]; then
            echo "No reference pack directory found; skipping"
            echo "SYSTEM_DRAWING_REF=" >> "$GITHUB_ENV"
            exit 0
          fi
          ref_path="$ref_dir/ref/net10.0/System.Drawing.Common.dll"
          if [ ! -f "$ref_path" ]; then
            echo "System.Drawing.Common reference not found at $ref_path"
            echo "SYSTEM_DRAWING_REF=" >> "$GITHUB_ENV"
            exit 0
          fi
          mv "$ref_path" "$ref_path.disabled"
          echo "SYSTEM_DRAWING_REF=$ref_path" >> "$GITHUB_ENV"
          echo "Temporarily moved $ref_path"

      - name: Determine package version
        id: version
        run: |
          version=$(grep -m1 'AssemblyInformationalVersion' CommonAssemblyInfo.cs | sed -E 's/.*"([^"]+)".*/\1/')
          if [ -z "$version" ]; then
            echo "Failed to determine version from CommonAssemblyInfo.cs" >&2
            exit 1
          fi
          echo "Plugin package version: $version"
          echo "package_version=$version" >> "$GITHUB_OUTPUT"

      - name: Prepare Touch-N-Stars frontend
        if: ${{ matrix.needs_frontend == true }}
        run: |
          plugin_dir=$(dirname "${{ matrix.project_path }}")
          tmp_dir="$plugin_dir/frontend-build"
          rm -rf "$tmp_dir" "$plugin_dir/app"
          git clone --branch "${{ matrix.frontend_branch }}" --single-branch "${{ matrix.frontend_repo }}" "$tmp_dir"
          pushd "$tmp_dir" >/dev/null
          npm install
          npm run build
          popd >/dev/null
          mv "$tmp_dir/dist" "$plugin_dir/app"

      - name: Restore plugin dependencies
        run: dotnet restore "${{ matrix.project_path }}"

      - name: Build plugin assemblies
        run: dotnet build "${{ matrix.project_path }}" -c ${{ env.BUILD_CONFIGURATION }} --no-restore

      - name: Stage plugin payload
        run: |
          plugin_dir=$(dirname "${{ matrix.project_path }}")
          build_root="$plugin_dir/bin/${{ env.BUILD_CONFIGURATION }}"
          tfm_dir=$(find "$build_root" -maxdepth 1 -type d -name 'net*' | head -n 1)
          if [ -z "$tfm_dir" ]; then
            echo "Could not locate target framework output under $build_root" >&2
            exit 1
          fi
          deb_root="${{ env.PLUGIN_DEB_BASE }}/${{ matrix.deb_package }}"
          install_root="$deb_root${{ env.PLUGINS_INSTALL_BASE }}/${{ matrix.install_folder }}"
          rm -rf "$deb_root"
          mkdir -p "$install_root"
          rsync -a "$tfm_dir/" "$install_root/"
          extra_libs="$plugin_dir/extra-libs"
          if [ -d "$extra_libs" ]; then
            find "$extra_libs" -maxdepth 1 -type f -name '*.dll' -exec cp {} "$install_root/" \;
          fi
          app_dir="$plugin_dir/app"
          if [ -d "$app_dir" ]; then
            rm -rf "$install_root/app"
            cp -a "$app_dir" "$install_root/app"
          fi

      - name: Build Debian package
        id: build_deb
        run: |
          deb_root="${{ env.PLUGIN_DEB_BASE }}/${{ matrix.deb_package }}"
          control_dir="$deb_root/DEBIAN"
          mkdir -p "$control_dir"
          installed_size=$(du -sk "$deb_root${{ env.PLUGINS_INSTALL_BASE }}" | cut -f1)
          {
            echo "Package: ${{ matrix.deb_package }}"
            echo "Version: ${{ steps.version.outputs.package_version }}"
            echo "Section: misc"
            echo "Priority: optional"
            echo "Architecture: ${{ env.DEB_ARCH }}"
            echo "Maintainer: N.I.N.A. Team"
            echo "Installed-Size: $installed_size"
            echo "Description: NINA plugin package for ${{ matrix.install_folder }}"
          } > "$control_dir/control"
          mkdir -p "${{ env.PLUGIN_ARTIFACT_DIR }}"
          deb_path="${{ env.PLUGIN_ARTIFACT_DIR }}/${{ matrix.deb_package }}_${{ steps.version.outputs.package_version }}_${{ env.DEB_ARCH }}.deb"
          dpkg-deb --build "$deb_root" "$deb_path"
          echo "deb_path=$deb_path" >> "$GITHUB_OUTPUT"

      - name: Generate plugin checksum
        id: checksum
        run: |
          deb_path="${{ steps.build_deb.outputs.deb_path }}"
          if [ ! -f "$deb_path" ]; then
            echo "Missing deb artifact at $deb_path" >&2
            exit 1
          fi
          sha_path="$deb_path.sha256"
          sha256sum "$deb_path" > "$sha_path"
          echo "sha_path=$sha_path" >> "$GITHUB_OUTPUT"

      - name: Upload plugin deb artifact
        uses: actions/upload-artifact@v4
        with:
          name: plugin-${{ matrix.install_folder }}
          path: |
            ${{ steps.build_deb.outputs.deb_path }}
            ${{ steps.checksum.outputs.sha_path }}
          retention-days: 30

      - name: Restore System.Drawing.Common reference
        if: always()
        shell: bash
        run: |
          ref_path="${SYSTEM_DRAWING_REF:-}"
          if [ -n "$ref_path" ] && [ -f "$ref_path.disabled" ]; then
            mv "$ref_path.disabled" "$ref_path"
            echo "Restored $ref_path"
          fi
