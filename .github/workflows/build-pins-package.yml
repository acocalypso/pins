name: Build Linux ARM64 Package

on:
  workflow_dispatch:

env:
  BUILD_CONFIGURATION: Release
  TARGET_RUNTIME: linux-arm64
  PLUGIN_TARGET_FRAMEWORK: net10.0
  INSTALL_DIRECTORY: /home/pi/pins
  PUBLISH_DIRECTORY: artifacts/publish
  PACKAGE_ROOT: artifacts/package
  PACKAGE_NAME: pins-linux-arm64
  DEB_ROOT: artifacts/debroot
  DEB_PACKAGE: pins
  DEB_ARCH: arm64
  PLUGINS_INSTALL_BASE: /home/pi/.local/share/NINA/Plugins/3.0.0
  PLUGIN_DEB_BASE: artifacts/plugin-debroot
  PLUGIN_ARTIFACT_DIR: artifacts/plugins
  PLUGIN_MSBUILD_ARGS: -p:DisableImplicitSystemDrawingReference=true -p:Platform=AnyCPU -p:PlatformTarget=AnyCPU -p:SatelliteResourceLanguages="en-US"
  PLUGIN_FAILURES_FILE: artifacts/plugin-build-failures.log

jobs:
  build-linux-arm64:
    name: Build Raspberry Pi package and plugin debs
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          lfs: true
          submodules: false

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libudev-dev

      - name: Clone plugins
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p NINA.Plugins

          # Clone N.I.N.A-Plugin-for-Touch-N-Stars (branch develop) -> NINA.Plugins/Touch-N-Stars
          rm -rf "NINA.Plugins/Touch-N-Stars"
          git clone --branch develop --single-branch https://github.com/nitr57/N.I.N.A-Plugin-for-Touch-N-Stars "NINA.Plugins/Touch-N-Stars"

          # Clone joko.nina.plugins -> NINA.Plugins/joko.nina.plugins
          rm -rf "NINA.Plugins/joko.nina.plugins"
          git clone https://github.com/nitr57/joko.nina.plugins "NINA.Plugins/joko.nina.plugins"

          # Clone ninaAPI -> NINA.Plugins/ninaAPI
          rm -rf "NINA.Plugins/ninaAPI"
          git clone https://github.com/nitr57/ninaAPI "NINA.Plugins/ninaAPI"

          # Clone nina.plugin.phd2tools -> NINA.Plugins/nina.plugin.phd2tools
          rm -rf "NINA.Plugins/nina.plugin.phd2tools"
          git clone https://github.com/nitr57/nina.plugin.phd2tools "NINA.Plugins/nina.plugin.phd2tools"

          # Clone nina.plugin.orbuculum -> NINA.Plugins/nina.plugin.orbuculum
          rm -rf "NINA.Plugins/nina.plugin.orbuculum"
          git clone https://github.com/nitr57/nina.plugin.orbuculum "NINA.Plugins/nina.plugin.orbuculum"

          # Clone nina.plugin.polaralignment -> NINA.Plugins/PolarAlignment
          rm -rf "NINA.Plugins/PolarAlignment"
          git clone https://github.com/nitr57/nina.plugin.polaralignment "NINA.Plugins/PolarAlignment"

          # Clone nina.plugin.livestack -> NINA.Plugins/LiveStack
          rm -rf "NINA.Plugins/LiveStack"
          git clone https://github.com/nitr57/nina.plugin.livestack "NINA.Plugins/LiveStack"

          # Clone NINA.Joko.Plugin.TenMicron -> NINA.Plugins/NINA.Joko.Plugin.TenMicron
          rm -rf "NINA.Plugins/NINA.Joko.Plugin.TenMicron"
          git clone https://github.com/nitr57/NINA.Joko.Plugin.TenMicron "NINA.Plugins/NINA.Joko.Plugin.TenMicron"

          # Clone pins.plugin -> NINA.Plugins/pins.plugin
          rm -rf "NINA.Plugins/pins.plugin"
          git clone https://github.com/nitr57/pins.plugin "NINA.Plugins/pins.plugin"


      - name: Populate NINA/External from zip bundle
        shell: bash
        run: |
          set -euo pipefail

          url="http://cloud.astro-narren.de/public.php/dav/files/7tEAZoEpCMCYyeX/?accept=zip"
          tmp_dir="$(mktemp -d)"
          zip_path="$tmp_dir/external.zip"
          unzip_dir="$tmp_dir/unzipped"

          echo "Downloading External bundle from: $url"
          curl -L --fail --retry 3 --retry-delay 5 -o "$zip_path" "$url"

          rm -rf "NINA/External"
          mkdir -p "NINA/External"
          unzip -q "$zip_path" -d "$unzip_dir"

          if [ ! -d "$unzip_dir/external" ]; then
            echo "Downloaded zip did not contain top-level folder 'external'" >&2
            echo "Top-level entries:" >&2
            (cd "$unzip_dir" && find . -mindepth 1 -maxdepth 2 -print | head -n 50) >&2
            exit 1
          fi

          rsync -a "$unzip_dir/external/" "NINA/External/"
          echo "External bundle extracted into NINA/External"

      - name: Setup .NET 10 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Restore linux-arm64 dependencies
        run: |
          dotnet restore NINA/NINA.csproj -r ${{ env.TARGET_RUNTIME }}
          dotnet restore System.Windows.Compat/System.Windows.Compat.csproj

      - name: Build System.Windows.Compat
        run: dotnet build System.Windows.Compat/System.Windows.Compat.csproj -c ${{ env.BUILD_CONFIGURATION }} --no-restore

      - name: Build linux-arm64 binaries
        run: dotnet build NINA/NINA.csproj -c ${{ env.BUILD_CONFIGURATION }} -r ${{ env.TARGET_RUNTIME }} --no-restore

      - name: Publish linux-arm64 payload
        run: dotnet publish NINA/NINA.csproj -c ${{ env.BUILD_CONFIGURATION }} -r ${{ env.TARGET_RUNTIME }} --no-build -o ${{ env.PUBLISH_DIRECTORY }}/${{ env.TARGET_RUNTIME }}

      - name: Include External native bundle in publish output
        shell: bash
        run: |
          set -euo pipefail

          publish_root="${{ env.PUBLISH_DIRECTORY }}/${{ env.TARGET_RUNTIME }}"

          if [ ! -d "NINA/External" ]; then
            echo "NINA/External not found; native bundle was not populated" >&2
            exit 1
          fi

          mkdir -p "$publish_root/External"
          rsync -a "NINA/External/" "$publish_root/External/"

          echo "Bundled native files staged under: $publish_root/External"

      - name: Determine package version
        id: version
        run: |
          base_version=$(grep -m1 'AssemblyInformationalVersion' CommonAssemblyInfo.cs | sed -E 's/.*"([^"]+)".*/\1/')
          if [ -z "$base_version" ]; then
            echo "Failed to determine version from CommonAssemblyInfo.cs" >&2
            exit 1
          fi

          # Ensure canary builds sort higher than previous nightly builds (p > n)
          base_version=$(echo "$base_version" | sed 's/-canary/-pins-canary/')

          # Ensure every CI build produces a unique, increasing Debian version.
          # Debian versions support a '+' build metadata suffix.
          version="${base_version}+${GITHUB_RUN_NUMBER}"

          echo "Base version: $base_version"
          echo "Package version: $version"
          echo "package_version=$version" >> "$GITHUB_OUTPUT"

      - name: Determine release version
        id: release_tag
        run: |
          release_date=$(date +%d%m%Y)
          release_version="v${release_date}-${GITHUB_RUN_NUMBER}"
          echo "Release version: $release_version"
          echo "Release date: $release_date"
          echo "RELEASE_DATE=$release_date" >> "$GITHUB_ENV"
          echo "RELEASE_VERSION=$release_version" >> "$GITHUB_ENV"
          echo "value=$release_version" >> "$GITHUB_OUTPUT"
          echo "date=$release_date" >> "$GITHUB_OUTPUT"

      - name: Stage /home/pi/pins layout
        run: |
          publish_root="${{ env.PUBLISH_DIRECTORY }}/${{ env.TARGET_RUNTIME }}"
          package_root="${{ env.PACKAGE_ROOT }}"
          rm -rf "$package_root"
          mkdir -p "$package_root/pins"
          rsync -a "$publish_root/" "$package_root/pins/"
          echo "${{ env.INSTALL_DIRECTORY }}" > "$package_root/pins/.install_path"

      - name: Replace System.Windows.dll in payload
        run: |
          compat_dll="System.Windows.Compat/bin/${{ env.BUILD_CONFIGURATION }}/net10.0/System.Windows.dll"
          target_dir="${{ env.PACKAGE_ROOT }}/pins"
          if [ ! -f "$compat_dll" ]; then
            echo "Compat build missing at $compat_dll" >&2
            exit 1
          fi
          if [ ! -d "$target_dir" ]; then
            echo "Target directory $target_dir not found" >&2
            exit 1
          fi
          cp -f "$compat_dll" "$target_dir/System.Windows.dll"

      - name: Prepare Debian filesystem
        run: |
          deb_root="${{ env.DEB_ROOT }}"
          install_root="$deb_root/home/pi/pins"
          rm -rf "$deb_root"
          mkdir -p "$install_root"
          rsync -a "${{ env.PACKAGE_ROOT }}/pins/" "$install_root/"

      - name: Add systemd service
        run: |
          deb_root="${{ env.DEB_ROOT }}"
          service_path="$deb_root/etc/systemd/system/pins.service"
          service_dir="$(dirname "$service_path")"
          mkdir -p "$service_dir"
          cp packaging/systemd/pins.service "$service_path"
          chmod 644 "$service_path"

      - name: Add Debian maintainer scripts
        run: |
          deb_root="${{ env.DEB_ROOT }}"
          control_dir="$deb_root/DEBIAN"
          mkdir -p "$control_dir"
          cp packaging/debian/postinst "$control_dir/postinst"
          cp packaging/debian/prerm "$control_dir/prerm"
          chmod 755 "$control_dir/postinst" "$control_dir/prerm"

      - name: Build Debian package
        run: |
          deb_root="${{ env.DEB_ROOT }}"
          control_dir="$deb_root/DEBIAN"
          mkdir -p "$control_dir"
          installed_size=$(du -sk "$deb_root/home/pi/pins" | cut -f1)
          {
            echo "Package: ${{ env.DEB_PACKAGE }}"
            echo "Version: ${{ steps.version.outputs.package_version }}"
            echo "Section: misc"
            echo "Priority: optional"
            echo "Architecture: ${{ env.DEB_ARCH }}"
            echo "Maintainer: PINS Team"
            echo "Installed-Size: $installed_size"
            echo "Description: Pins build for Raspberry Pi"
          } > "$control_dir/control"
          dpkg-deb --root-owner-group --build "$deb_root" "artifacts/${{ env.DEB_PACKAGE }}_${{ steps.version.outputs.package_version }}_${{ env.DEB_ARCH }}_${{ steps.release_tag.outputs.date }}.deb"

      - name: Generate SHA256 checksums
        run: |
          sha256sum "artifacts/${{ env.DEB_PACKAGE }}_${{ steps.version.outputs.package_version }}_${{ env.DEB_ARCH }}_${{ steps.release_tag.outputs.date }}.deb" > "artifacts/${{ env.DEB_PACKAGE }}_${{ steps.version.outputs.package_version }}_${{ env.DEB_ARCH }}_${{ steps.release_tag.outputs.date }}.deb.sha256"

      - name: Upload Raspberry Pi package
        uses: actions/upload-artifact@v4
        with:
          name: linux-arm64-package
          path: |
            artifacts/${{ env.DEB_PACKAGE }}_${{ steps.version.outputs.package_version }}_${{ env.DEB_ARCH }}_${{ steps.release_tag.outputs.date }}.deb
            artifacts/${{ env.DEB_PACKAGE }}_${{ steps.version.outputs.package_version }}_${{ env.DEB_ARCH }}_${{ steps.release_tag.outputs.date }}.deb.sha256
          retention-days: 30

      - name: Checkout pinsdaemon
        uses: actions/checkout@v4
        with:
          repository: acocalypso/pinsdaemon
          path: pinsdaemon

      - name: Build PinsDaemon Package
        working-directory: pinsdaemon
        run: |
          mkdir -p build/opt/pinsdaemon
          mkdir -p build/usr/local/bin
          mkdir -p build/etc/systemd/system
          mkdir -p build/etc/sudoers.d
          mkdir -p build/DEBIAN
          
          # App files
          cp -r app build/opt/pinsdaemon/
          cp requirements.txt build/opt/pinsdaemon/
          cp README.md build/opt/pinsdaemon/
          
          # Scripts
          cp scripts/system-upgrade.sh build/usr/local/bin/
          cp scripts/manage-samba.sh build/usr/local/bin/
          cp scripts/wifi-connect.sh build/usr/local/bin/
          cp scripts/wifi-scan.py build/usr/local/bin/
          
          # Systemd
          cp systemd/sysupdate-api.service build/etc/systemd/system/
          
          # Sudoers
          cp packaging/sudoers build/etc/sudoers.d/sysupdate-api
          
          # Debian control files
          cp packaging/DEBIAN/* build/DEBIAN/
          chmod 755 build/DEBIAN/postinst
          chmod 755 build/DEBIAN/prerm

          # Versioning
          BASE_VERSION=$(grep "^Version:" packaging/DEBIAN/control | awk '{print $2}')
          FULL_VERSION="${BASE_VERSION}-${{ github.run_number }}"
          FILENAME="pinsdaemon_${FULL_VERSION}_arm64.deb"
          
          echo "Setting version to $FULL_VERSION"
          sed -i "s/^Version:.*/Version: $FULL_VERSION/" build/DEBIAN/control
          
          # Build
          dpkg-deb --root-owner-group --build build "$FILENAME"
          
          # Move to artifacts
          mkdir -p ../artifacts
          mv "$FILENAME" ../artifacts/

      - name: Prepare plugin packaging workspace
        run: |
          rm -rf "${{ env.PLUGIN_DEB_BASE }}"
          mkdir -p "${{ env.PLUGIN_ARTIFACT_DIR }}"
          mkdir -p "$(dirname "${{ env.PLUGIN_FAILURES_FILE }}")"
          rm -f "${{ env.PLUGIN_FAILURES_FILE }}"

      - name: Build HocusFocus plugin deb
        shell: bash
        run: |
          set -euo pipefail
          project_path="NINA.Plugins/joko.nina.plugins/Joko.NINA.Plugins/Joko.NINA.Plugins.HocusFocus/Joko.NINA.Plugins.HocusFocus.csproj"
          install_folder="joko.nina.plugins"
          deb_package="pins-plugin-joko"
          plugin_dir=$(dirname "$project_path")
          set +e
          (
            set -euo pipefail
            assembly_info="$plugin_dir/Properties/AssemblyInfo.cs"
            if [ ! -f "$assembly_info" ]; then
              assembly_info=$(find "$plugin_dir" -name AssemblyInfo.cs | head -n 1)
            fi
            if [ -z "$assembly_info" ] || [ ! -f "$assembly_info" ]; then
              echo "AssemblyInfo.cs not found for $deb_package" >&2
              exit 1
            fi
            assembly_version_line=$(grep -E 'AssemblyVersion' "$assembly_info" || true)
            plugin_version=$(echo "$assembly_version_line" | sed -E 's/.*"([^\"]+)".*/\1/')
            if [ -z "$plugin_version" ]; then
              echo "AssemblyVersion not found in $assembly_info" >&2
              exit 1
            fi

            # Ensure every CI build produces a unique, increasing Debian version.
            plugin_version="${plugin_version}+${GITHUB_RUN_NUMBER}"
            dotnet restore "$project_path" -r ${{ env.TARGET_RUNTIME }}
            dotnet build "$project_path" -c ${{ env.BUILD_CONFIGURATION }} -f ${{ env.PLUGIN_TARGET_FRAMEWORK }} -r ${{ env.TARGET_RUNTIME }} --no-restore ${{ env.PLUGIN_MSBUILD_ARGS }}
            tfm_dir="$plugin_dir/bin/${{ env.BUILD_CONFIGURATION }}/${{ env.PLUGIN_TARGET_FRAMEWORK }}/${{ env.TARGET_RUNTIME }}"
            if [ ! -d "$tfm_dir" ]; then
              echo "Expected RID output folder not found at: $tfm_dir" >&2
              echo "Falling back to first 'net*' folder under bin/${{ env.BUILD_CONFIGURATION }}" >&2
              build_root="$plugin_dir/bin/${{ env.BUILD_CONFIGURATION }}"
              tfm_dir=$(find "$build_root" -maxdepth 1 -type d -name 'net*' | head -n 1)
              if [ -z "$tfm_dir" ]; then
                echo "Could not locate target framework output under $build_root" >&2
                exit 1
              fi
            fi
            deb_root="${{ env.PLUGIN_DEB_BASE }}/$deb_package"
            install_root="$deb_root${{ env.PLUGINS_INSTALL_BASE }}/$install_folder"
            rm -rf "$deb_root"
            mkdir -p "$install_root"
            # Exclude core NINA files from plugin package
            core_publish_dir="${{ env.PUBLISH_DIRECTORY }}/${{ env.TARGET_RUNTIME }}"
            exclude_list="$(pwd)/core_excludes.txt"
            (cd "$core_publish_dir" && find . -type f -printf "%P\n") > "$exclude_list"
            rsync -a --exclude-from="$exclude_list" "$tfm_dir/" "$install_root/"
            rm -f "$exclude_list"
            find "$install_root" -mindepth 1 -maxdepth 1 -type d -name "??-??" -exec rm -rf {} +
            find "$install_root" -mindepth 1 -maxdepth 1 -type d -name "??" -exec rm -rf {} +
            extra_libs="$plugin_dir/extra-libs"
            if [ -d "$extra_libs" ]; then
              find "$extra_libs" -maxdepth 1 -type f -name '*.dll' -exec cp {} "$install_root/" \;
            fi
            app_dir="$plugin_dir/app"
            if [ -d "$app_dir" ]; then
              rm -rf "$install_root/app"
              cp -a "$app_dir" "$install_root/app"
            fi
            control_dir="$deb_root/DEBIAN"
            mkdir -p "$control_dir"
            installed_size=$(du -sk "$deb_root${{ env.PLUGINS_INSTALL_BASE }}" | cut -f1)
            {
              echo "Package: $deb_package"
              echo "Version: $plugin_version"
              echo "Section: misc"
              echo "Priority: optional"
              echo "Architecture: ${{ env.DEB_ARCH }}"
              echo "Maintainer: PINS Team"
              echo "Installed-Size: $installed_size"
              echo "Description: NINA plugin package for $install_folder"
            } > "$control_dir/control"
            {
              echo "#!/bin/sh"
              echo "set -e"
              echo "chown -R pi:pi ${{ env.PLUGINS_INSTALL_BASE }}/$install_folder || true"
              echo "exit 0"
            } > "$control_dir/postinst"
            chmod 755 "$control_dir/postinst"
            deb_path="${{ env.PLUGIN_ARTIFACT_DIR }}/${deb_package}_${plugin_version}_${{ env.DEB_ARCH }}_${{ steps.release_tag.outputs.date }}.deb"
            dpkg-deb --root-owner-group --build "$deb_root" "$deb_path"
            sha256sum "$deb_path" > "$deb_path.sha256"
          )
          rc=$?
          set -e
          if [ $rc -eq 0 ]; then
            echo "Built $deb_package"
          else
            echo "$deb_package" | tee -a "${{ env.PLUGIN_FAILURES_FILE }}"
          fi

      - name: Build LiveStack plugin deb
        shell: bash
        run: |
          set -euo pipefail
          project_path="NINA.Plugins/LiveStack/nina.plugin.livestack.csproj"
          install_folder="LiveStack"
          deb_package="pins-plugin-livestack"
          plugin_dir=$(dirname "$project_path")
          set +e
          (
            set -euo pipefail
            assembly_info="$plugin_dir/Properties/AssemblyInfo.cs"
            if [ ! -f "$assembly_info" ]; then
              assembly_info=$(find "$plugin_dir" -name AssemblyInfo.cs | head -n 1)
            fi
            if [ -z "$assembly_info" ] || [ ! -f "$assembly_info" ]; then
              echo "AssemblyInfo.cs not found for $deb_package" >&2
              exit 1
            fi
            assembly_version_line=$(grep -E 'AssemblyVersion' "$assembly_info" || true)
            plugin_version=$(echo "$assembly_version_line" | sed -E 's/.*"([^\"]+)".*/\1/')
            if [ -z "$plugin_version" ]; then
              echo "AssemblyVersion not found in $assembly_info" >&2
              exit 1
            fi

            # Ensure every CI build produces a unique, increasing Debian version.
            plugin_version="${plugin_version}+${GITHUB_RUN_NUMBER}"
            dotnet restore "$project_path" -r ${{ env.TARGET_RUNTIME }}
            dotnet build "$project_path" -c ${{ env.BUILD_CONFIGURATION }} -f ${{ env.PLUGIN_TARGET_FRAMEWORK }} -r ${{ env.TARGET_RUNTIME }} --no-restore ${{ env.PLUGIN_MSBUILD_ARGS }}
            tfm_dir="$plugin_dir/bin/${{ env.BUILD_CONFIGURATION }}/${{ env.PLUGIN_TARGET_FRAMEWORK }}/${{ env.TARGET_RUNTIME }}"
            if [ ! -d "$tfm_dir" ]; then
              echo "Expected RID output folder not found at: $tfm_dir" >&2
              echo "Falling back to first 'net*' folder under bin/${{ env.BUILD_CONFIGURATION }}" >&2
              build_root="$plugin_dir/bin/${{ env.BUILD_CONFIGURATION }}"
              tfm_dir=$(find "$build_root" -maxdepth 1 -type d -name 'net*' | head -n 1)
              if [ -z "$tfm_dir" ]; then
                echo "Could not locate target framework output under $build_root" >&2
                exit 1
              fi
            fi
            deb_root="${{ env.PLUGIN_DEB_BASE }}/$deb_package"
            install_root="$deb_root${{ env.PLUGINS_INSTALL_BASE }}/$install_folder"
            rm -rf "$deb_root"
            mkdir -p "$install_root"
            # Exclude core NINA files from plugin package
            core_publish_dir="${{ env.PUBLISH_DIRECTORY }}/${{ env.TARGET_RUNTIME }}"
            exclude_list="$(pwd)/core_excludes.txt"
            (cd "$core_publish_dir" && find . -type f -printf "%P\n") > "$exclude_list"
            rsync -a --exclude-from="$exclude_list" "$tfm_dir/" "$install_root/"
            rm -f "$exclude_list"
            find "$install_root" -mindepth 1 -maxdepth 1 -type d -name "??-??" -exec rm -rf {} +
            find "$install_root" -mindepth 1 -maxdepth 1 -type d -name "??" -exec rm -rf {} +
            extra_libs="$plugin_dir/extra-libs"
            if [ -d "$extra_libs" ]; then
              find "$extra_libs" -maxdepth 1 -type f -name '*.dll' -exec cp {} "$install_root/" \;
            fi
            app_dir="$plugin_dir/app"
            if [ -d "$app_dir" ]; then
              rm -rf "$install_root/app"
              cp -a "$app_dir" "$install_root/app"
            fi
            control_dir="$deb_root/DEBIAN"
            mkdir -p "$control_dir"
            installed_size=$(du -sk "$deb_root${{ env.PLUGINS_INSTALL_BASE }}" | cut -f1)
            {
              echo "Package: $deb_package"
              echo "Version: $plugin_version"
              echo "Section: misc"
              echo "Priority: optional"
              echo "Architecture: ${{ env.DEB_ARCH }}"
              echo "Maintainer: PINS Team"
              echo "Installed-Size: $installed_size"
              echo "Description: NINA plugin package for $install_folder"
            } > "$control_dir/control"
            {
              echo "#!/bin/sh"
              echo "set -e"
              echo "chown -R pi:pi ${{ env.PLUGINS_INSTALL_BASE }}/$install_folder || true"
              echo "exit 0"
            } > "$control_dir/postinst"
            chmod 755 "$control_dir/postinst"
            deb_path="${{ env.PLUGIN_ARTIFACT_DIR }}/${deb_package}_${plugin_version}_${{ env.DEB_ARCH }}_${{ steps.release_tag.outputs.date }}.deb"
            dpkg-deb --root-owner-group --build "$deb_root" "$deb_path"
            sha256sum "$deb_path" > "$deb_path.sha256"
          )
          rc=$?
          set -e
          if [ $rc -eq 0 ]; then
            echo "Built $deb_package"
          else
            echo "$deb_package" | tee -a "${{ env.PLUGIN_FAILURES_FILE }}"
          fi

      - name: Build ninaAPI plugin deb
        shell: bash
        run: |
          set -euo pipefail
          project_path="NINA.Plugins/ninaAPI/ninaAPI/ninaAPI.csproj"
          install_folder="ninaAPI"
          deb_package="pins-plugin-ninaapi"
          plugin_dir=$(dirname "$project_path")
          set +e
          (
            set -euo pipefail
            assembly_info="$plugin_dir/Properties/AssemblyInfo.cs"
            if [ ! -f "$assembly_info" ]; then
              assembly_info=$(find "$plugin_dir" -name AssemblyInfo.cs | head -n 1)
            fi
            if [ -z "$assembly_info" ] || [ ! -f "$assembly_info" ]; then
              echo "AssemblyInfo.cs not found for $deb_package" >&2
              exit 1
            fi
            assembly_version_line=$(grep -E 'AssemblyVersion' "$assembly_info" || true)
            plugin_version=$(echo "$assembly_version_line" | sed -E 's/.*"([^\"]+)".*/\1/')
            if [ -z "$plugin_version" ]; then
              echo "AssemblyVersion not found in $assembly_info" >&2
              exit 1
            fi

            # Ensure every CI build produces a unique, increasing Debian version.
            plugin_version="${plugin_version}+${GITHUB_RUN_NUMBER}"
            dotnet restore "$project_path" -r ${{ env.TARGET_RUNTIME }}
            dotnet build "$project_path" -c ${{ env.BUILD_CONFIGURATION }} -f ${{ env.PLUGIN_TARGET_FRAMEWORK }} -r ${{ env.TARGET_RUNTIME }} --no-restore ${{ env.PLUGIN_MSBUILD_ARGS }}
            tfm_dir="$plugin_dir/bin/${{ env.BUILD_CONFIGURATION }}/${{ env.PLUGIN_TARGET_FRAMEWORK }}/${{ env.TARGET_RUNTIME }}"
            if [ ! -d "$tfm_dir" ]; then
              echo "Expected RID output folder not found at: $tfm_dir" >&2
              echo "Falling back to first 'net*' folder under bin/${{ env.BUILD_CONFIGURATION }}" >&2
              build_root="$plugin_dir/bin/${{ env.BUILD_CONFIGURATION }}"
              tfm_dir=$(find "$build_root" -maxdepth 1 -type d -name 'net*' | head -n 1)
              if [ -z "$tfm_dir" ]; then
                echo "Could not locate target framework output under $build_root" >&2
                exit 1
              fi
            fi
            deb_root="${{ env.PLUGIN_DEB_BASE }}/$deb_package"
            install_root="$deb_root${{ env.PLUGINS_INSTALL_BASE }}/$install_folder"
            rm -rf "$deb_root"
            mkdir -p "$install_root"
            # Exclude core NINA files from plugin package
            core_publish_dir="${{ env.PUBLISH_DIRECTORY }}/${{ env.TARGET_RUNTIME }}"
            exclude_list="$(pwd)/core_excludes.txt"
            (cd "$core_publish_dir" && find . -type f -printf "%P\n") > "$exclude_list"
            rsync -a --exclude-from="$exclude_list" "$tfm_dir/" "$install_root/"
            rm -f "$exclude_list"
            find "$install_root" -mindepth 1 -maxdepth 1 -type d -name "??-??" -exec rm -rf {} +
            find "$install_root" -mindepth 1 -maxdepth 1 -type d -name "??" -exec rm -rf {} +
            extra_libs="$plugin_dir/extra-libs"
            if [ -d "$extra_libs" ]; then
              find "$extra_libs" -maxdepth 1 -type f -name '*.dll' -exec cp {} "$install_root/" \;
            fi
            app_dir="$plugin_dir/app"
            if [ -d "$app_dir" ]; then
              rm -rf "$install_root/app"
              cp -a "$app_dir" "$install_root/app"
            fi
            control_dir="$deb_root/DEBIAN"
            mkdir -p "$control_dir"
            installed_size=$(du -sk "$deb_root${{ env.PLUGINS_INSTALL_BASE }}" | cut -f1)
            {
              echo "Package: $deb_package"
              echo "Version: $plugin_version"
              echo "Section: misc"
              echo "Priority: optional"
              echo "Architecture: ${{ env.DEB_ARCH }}"
              echo "Maintainer: PINS Team"
              echo "Installed-Size: $installed_size"
              echo "Description: NINA plugin package for $install_folder"
            } > "$control_dir/control"
            {
              echo "#!/bin/sh"
              echo "set -e"
              echo "chown -R pi:pi ${{ env.PLUGINS_INSTALL_BASE }}/$install_folder || true"
              echo "exit 0"
            } > "$control_dir/postinst"
            chmod 755 "$control_dir/postinst"
            deb_path="${{ env.PLUGIN_ARTIFACT_DIR }}/${deb_package}_${plugin_version}_${{ env.DEB_ARCH }}_${{ steps.release_tag.outputs.date }}.deb"
            dpkg-deb --root-owner-group --build "$deb_root" "$deb_path"
            sha256sum "$deb_path" > "$deb_path.sha256"
          )
          rc=$?
          set -e
          if [ $rc -eq 0 ]; then
            echo "Built $deb_package"
          else
            echo "$deb_package" | tee -a "${{ env.PLUGIN_FAILURES_FILE }}"
          fi

      - name: Build Orbuculum plugin deb
        shell: bash
        run: |
          set -euo pipefail
          project_path="NINA.Plugins/nina.plugin.orbuculum/Orbuculum/Orbuculum.csproj"
          install_folder="Orbuculum"
          deb_package="pins-plugin-orbuculum"
          plugin_dir=$(dirname "$project_path")
          set +e
          (
            set -euo pipefail
            assembly_info="$plugin_dir/Properties/AssemblyInfo.cs"
            if [ ! -f "$assembly_info" ]; then
              assembly_info=$(find "$plugin_dir" -name AssemblyInfo.cs | head -n 1)
            fi
            if [ -z "$assembly_info" ] || [ ! -f "$assembly_info" ]; then
              echo "AssemblyInfo.cs not found for $deb_package" >&2
              exit 1
            fi
            assembly_version_line=$(grep -E '^\s*\[assembly:\s*AssemblyVersion' "$assembly_info" || true)
            plugin_version=$(echo "$assembly_version_line" | sed -E 's/.*"([^\"]+)".*/\1/')
            if [ -z "$plugin_version" ]; then
              echo "AssemblyVersion not found in $assembly_info" >&2
              exit 1
            fi

            # Ensure every CI build produces a unique, increasing Debian version.
            plugin_version="${plugin_version}+${GITHUB_RUN_NUMBER}"
            dotnet restore "$project_path" -r ${{ env.TARGET_RUNTIME }}
            dotnet build "$project_path" -c ${{ env.BUILD_CONFIGURATION }} -f ${{ env.PLUGIN_TARGET_FRAMEWORK }} -r ${{ env.TARGET_RUNTIME }} --no-restore ${{ env.PLUGIN_MSBUILD_ARGS }}
            tfm_dir="$plugin_dir/bin/${{ env.BUILD_CONFIGURATION }}/${{ env.PLUGIN_TARGET_FRAMEWORK }}/${{ env.TARGET_RUNTIME }}"
            if [ ! -d "$tfm_dir" ]; then
              echo "Expected RID output folder not found at: $tfm_dir" >&2
              echo "Falling back to first 'net*' folder under bin/${{ env.BUILD_CONFIGURATION }}" >&2
              build_root="$plugin_dir/bin/${{ env.BUILD_CONFIGURATION }}"
              tfm_dir=$(find "$build_root" -maxdepth 1 -type d -name 'net*' | head -n 1)
              if [ -z "$tfm_dir" ]; then
                echo "Could not locate target framework output under $build_root" >&2
                exit 1
              fi
            fi
            deb_root="${{ env.PLUGIN_DEB_BASE }}/$deb_package"
            install_root="$deb_root${{ env.PLUGINS_INSTALL_BASE }}/$install_folder"
            rm -rf "$deb_root"
            mkdir -p "$install_root"
            # Exclude core NINA files from plugin package
            core_publish_dir="${{ env.PUBLISH_DIRECTORY }}/${{ env.TARGET_RUNTIME }}"
            exclude_list="$(pwd)/core_excludes.txt"
            (cd "$core_publish_dir" && find . -type f -printf "%P\n") > "$exclude_list"
            rsync -a --exclude-from="$exclude_list" "$tfm_dir/" "$install_root/"
            rm -f "$exclude_list"
            find "$install_root" -mindepth 1 -maxdepth 1 -type d -name "??-??" -exec rm -rf {} +
            find "$install_root" -mindepth 1 -maxdepth 1 -type d -name "??" -exec rm -rf {} +
            extra_libs="$plugin_dir/extra-libs"
            if [ -d "$extra_libs" ]; then
              find "$extra_libs" -maxdepth 1 -type f -name '*.dll' -exec cp {} "$install_root/" \;
            fi
            app_dir="$plugin_dir/app"
            if [ -d "$app_dir" ]; then
              rm -rf "$install_root/app"
              cp -a "$app_dir" "$install_root/app"
            fi
            control_dir="$deb_root/DEBIAN"
            mkdir -p "$control_dir"
            installed_size=$(du -sk "$deb_root${{ env.PLUGINS_INSTALL_BASE }}" | cut -f1)
            {
              echo "Package: $deb_package"
              echo "Version: $plugin_version"
              echo "Section: misc"
              echo "Priority: optional"
              echo "Architecture: ${{ env.DEB_ARCH }}"
              echo "Maintainer: PINS Team"
              echo "Installed-Size: $installed_size"
              echo "Description: NINA plugin package for $install_folder"
            } > "$control_dir/control"
            {
              echo "#!/bin/sh"
              echo "set -e"
              echo "chown -R pi:pi ${{ env.PLUGINS_INSTALL_BASE }}/$install_folder || true"
              echo "exit 0"
            } > "$control_dir/postinst"
            chmod 755 "$control_dir/postinst"
            deb_path="${{ env.PLUGIN_ARTIFACT_DIR }}/${deb_package}_${plugin_version}_${{ env.DEB_ARCH }}_${{ steps.release_tag.outputs.date }}.deb"
            dpkg-deb --root-owner-group --build "$deb_root" "$deb_path"
            sha256sum "$deb_path" > "$deb_path.sha256"
          )
          rc=$?
          set -e
          if [ $rc -eq 0 ]; then
            echo "Built $deb_package"
          else
            echo "$deb_package" | tee -a "${{ env.PLUGIN_FAILURES_FILE }}"
          fi

      - name: Build Phd2Tools plugin deb
        shell: bash
        run: |
          set -euo pipefail
          project_path="NINA.Plugins/nina.plugin.phd2tools/nina.plugin.phd2tools.csproj"
          install_folder="Phd2 Tools"
          deb_package="pins-plugin-phd2tools"
          plugin_dir=$(dirname "$project_path")
          set +e
          (
            set -euo pipefail
            assembly_info="$plugin_dir/Properties/AssemblyInfo.cs"
            if [ ! -f "$assembly_info" ]; then
              assembly_info=$(find "$plugin_dir" -name AssemblyInfo.cs | head -n 1)
            fi
            if [ -z "$assembly_info" ] || [ ! -f "$assembly_info" ]; then
              echo "AssemblyInfo.cs not found for $deb_package" >&2
              exit 1
            fi
            
            # Try robust grep first (for active attribute)
            assembly_version_line=$(grep -E '^\s*\[assembly:\s*AssemblyVersion' "$assembly_info" || true)
            # Fallback to loose grep if robust found nothing (e.g. commented out)
            if [ -z "$assembly_version_line" ]; then
                assembly_version_line=$(grep -E 'AssemblyVersion' "$assembly_info" || true)
            fi

            plugin_version=$(echo "$assembly_version_line" | sed -E 's/.*"([^\"]+)".*/\1/')
            if [ -z "$plugin_version" ]; then
              echo "AssemblyVersion not found in $assembly_info" >&2
              exit 1
            fi

            # Ensure every CI build produces a unique, increasing Debian version.
            plugin_version="${plugin_version}+${GITHUB_RUN_NUMBER}"
            dotnet restore "$project_path" -r ${{ env.TARGET_RUNTIME }}
            dotnet build "$project_path" -c ${{ env.BUILD_CONFIGURATION }} -f ${{ env.PLUGIN_TARGET_FRAMEWORK }} -r ${{ env.TARGET_RUNTIME }} --no-restore ${{ env.PLUGIN_MSBUILD_ARGS }}
            tfm_dir="$plugin_dir/bin/${{ env.BUILD_CONFIGURATION }}/${{ env.PLUGIN_TARGET_FRAMEWORK }}/${{ env.TARGET_RUNTIME }}"
            if [ ! -d "$tfm_dir" ]; then
              echo "Expected RID output folder not found at: $tfm_dir" >&2
              echo "Falling back to first 'net*' folder under bin/${{ env.BUILD_CONFIGURATION }}" >&2
              build_root="$plugin_dir/bin/${{ env.BUILD_CONFIGURATION }}"
              tfm_dir=$(find "$build_root" -maxdepth 1 -type d -name 'net*' | head -n 1)
              if [ -z "$tfm_dir" ]; then
                echo "Could not locate target framework output under $build_root" >&2
                exit 1
              fi
            fi
            deb_root="${{ env.PLUGIN_DEB_BASE }}/$deb_package"
            install_root="$deb_root${{ env.PLUGINS_INSTALL_BASE }}/$install_folder"
            rm -rf "$deb_root"
            mkdir -p "$install_root"
            # Exclude core NINA files from plugin package
            core_publish_dir="${{ env.PUBLISH_DIRECTORY }}/${{ env.TARGET_RUNTIME }}"
            exclude_list="$(pwd)/core_excludes.txt"
            (cd "$core_publish_dir" && find . -type f -printf "%P\n") > "$exclude_list"
            rsync -a --exclude-from="$exclude_list" "$tfm_dir/" "$install_root/"
            rm -f "$exclude_list"
            find "$install_root" -mindepth 1 -maxdepth 1 -type d -name "??-??" -exec rm -rf {} +
            find "$install_root" -mindepth 1 -maxdepth 1 -type d -name "??" -exec rm -rf {} +
            extra_libs="$plugin_dir/extra-libs"
            if [ -d "$extra_libs" ]; then
              find "$extra_libs" -maxdepth 1 -type f -name '*.dll' -exec cp {} "$install_root/" \;
            fi
            app_dir="$plugin_dir/app"
            if [ -d "$app_dir" ]; then
              rm -rf "$install_root/app"
              cp -a "$app_dir" "$install_root/app"
            fi
            control_dir="$deb_root/DEBIAN"
            mkdir -p "$control_dir"
            installed_size=$(du -sk "$deb_root${{ env.PLUGINS_INSTALL_BASE }}" | cut -f1)
            {
              echo "Package: $deb_package"
              echo "Version: $plugin_version"
              echo "Section: misc"
              echo "Priority: optional"
              echo "Architecture: ${{ env.DEB_ARCH }}"
              echo "Maintainer: PINS Team"
              echo "Installed-Size: $installed_size"
              echo "Description: NINA plugin package for $install_folder"
            } > "$control_dir/control"
            {
              echo "#!/bin/sh"
              echo "set -e"
              echo "chown -R pi:pi \"${{ env.PLUGINS_INSTALL_BASE }}/$install_folder\" || true"
              echo "exit 0"
            } > "$control_dir/postinst"
            chmod 755 "$control_dir/postinst"
            deb_path="${{ env.PLUGIN_ARTIFACT_DIR }}/${deb_package}_${plugin_version}_${{ env.DEB_ARCH }}_${{ steps.release_tag.outputs.date }}.deb"
            dpkg-deb --root-owner-group --build "$deb_root" "$deb_path"
            sha256sum "$deb_path" > "$deb_path.sha256"
          )
          rc=$?
          set -e
          if [ $rc -eq 0 ]; then
            echo "Built $deb_package"
          else
            echo "$deb_package" | tee -a "${{ env.PLUGIN_FAILURES_FILE }}"
          fi

      - name: Build PolarAlignment plugin deb
        shell: bash
        run: |
          set -euo pipefail
          project_path="NINA.Plugins/PolarAlignment/PolarAlignment/NINA.Plugins.PolarAlignment.csproj"
          install_folder="PolarAlignment"
          deb_package="pins-plugin-polaralignment"
          plugin_dir=$(dirname "$project_path")
          set +e
          (
            set -euo pipefail
            assembly_info="$plugin_dir/Properties/AssemblyInfo.cs"
            if [ ! -f "$assembly_info" ]; then
              assembly_info=$(find "$plugin_dir" -name AssemblyInfo.cs | head -n 1)
            fi
            if [ -z "$assembly_info" ] || [ ! -f "$assembly_info" ]; then
              echo "AssemblyInfo.cs not found for $deb_package" >&2
              exit 1
            fi
            assembly_version_line=$(grep -E 'AssemblyVersion' "$assembly_info" || true)
            plugin_version=$(echo "$assembly_version_line" | sed -E 's/.*"([^\"]+)".*/\1/')
            if [ -z "$plugin_version" ]; then
              echo "AssemblyVersion not found in $assembly_info" >&2
              exit 1
            fi

            # Ensure every CI build produces a unique, increasing Debian version.
            plugin_version="${plugin_version}+${GITHUB_RUN_NUMBER}"
            dotnet restore "$project_path" -r ${{ env.TARGET_RUNTIME }}
            dotnet build "$project_path" -c ${{ env.BUILD_CONFIGURATION }} -f ${{ env.PLUGIN_TARGET_FRAMEWORK }} -r ${{ env.TARGET_RUNTIME }} --no-restore ${{ env.PLUGIN_MSBUILD_ARGS }}
            tfm_dir="$plugin_dir/bin/${{ env.BUILD_CONFIGURATION }}/${{ env.PLUGIN_TARGET_FRAMEWORK }}/${{ env.TARGET_RUNTIME }}"
            if [ ! -d "$tfm_dir" ]; then
              echo "Expected RID output folder not found at: $tfm_dir" >&2
              echo "Falling back to first 'net*' folder under bin/${{ env.BUILD_CONFIGURATION }}" >&2
              build_root="$plugin_dir/bin/${{ env.BUILD_CONFIGURATION }}"
              tfm_dir=$(find "$build_root" -maxdepth 1 -type d -name 'net*' | head -n 1)
              if [ -z "$tfm_dir" ]; then
                echo "Could not locate target framework output under $build_root" >&2
                exit 1
              fi
            fi
            deb_root="${{ env.PLUGIN_DEB_BASE }}/$deb_package"
            install_root="$deb_root${{ env.PLUGINS_INSTALL_BASE }}/$install_folder"
            rm -rf "$deb_root"
            mkdir -p "$install_root"
            # Exclude core NINA files from plugin package
            core_publish_dir="${{ env.PUBLISH_DIRECTORY }}/${{ env.TARGET_RUNTIME }}"
            exclude_list="$(pwd)/core_excludes.txt"
            (cd "$core_publish_dir" && find . -type f -printf "%P\n") > "$exclude_list"
            rsync -a --exclude-from="$exclude_list" "$tfm_dir/" "$install_root/"
            rm -f "$exclude_list"
            find "$install_root" -mindepth 1 -maxdepth 1 -type d -name "??-??" -exec rm -rf {} +
            find "$install_root" -mindepth 1 -maxdepth 1 -type d -name "??" -exec rm -rf {} +
            extra_libs="$plugin_dir/extra-libs"
            if [ -d "$extra_libs" ]; then
              find "$extra_libs" -maxdepth 1 -type f -name '*.dll' -exec cp {} "$install_root/" \;
            fi
            app_dir="$plugin_dir/app"
            if [ -d "$app_dir" ]; then
              rm -rf "$install_root/app"
              cp -a "$app_dir" "$install_root/app"
            fi
            control_dir="$deb_root/DEBIAN"
            mkdir -p "$control_dir"
            installed_size=$(du -sk "$deb_root${{ env.PLUGINS_INSTALL_BASE }}" | cut -f1)
            {
              echo "Package: $deb_package"
              echo "Version: $plugin_version"
              echo "Section: misc"
              echo "Priority: optional"
              echo "Architecture: ${{ env.DEB_ARCH }}"
              echo "Maintainer: PINS Team"
              echo "Installed-Size: $installed_size"
              echo "Description: NINA plugin package for $install_folder"
            } > "$control_dir/control"
            {
              echo "#!/bin/sh"
              echo "set -e"
              echo "chown -R pi:pi ${{ env.PLUGINS_INSTALL_BASE }}/$install_folder || true"
              echo "exit 0"
            } > "$control_dir/postinst"
            chmod 755 "$control_dir/postinst"
            deb_path="${{ env.PLUGIN_ARTIFACT_DIR }}/${deb_package}_${plugin_version}_${{ env.DEB_ARCH }}_${{ steps.release_tag.outputs.date }}.deb"
            dpkg-deb --root-owner-group --build "$deb_root" "$deb_path"
            sha256sum "$deb_path" > "$deb_path.sha256"
          )
          rc=$?
          set -e
          if [ $rc -eq 0 ]; then
            echo "Built $deb_package"
          else
            echo "$deb_package" | tee -a "${{ env.PLUGIN_FAILURES_FILE }}"
          fi

      - name: Build Touch-N-Stars plugin deb
        shell: bash
        run: |
          set -euo pipefail
          project_path="NINA.Plugins/Touch-N-Stars/Touch-N-Stars/Touch-N-Stars.csproj"
          install_folder="Touch-N-Stars"
          deb_package="pins-plugin-touch-n-stars"
          plugin_dir=$(dirname "$project_path")
          tmp_dir="$plugin_dir/frontend-build"
          set +e
          (
            set -euo pipefail
            assembly_info="$plugin_dir/Properties/AssemblyInfo.cs"
            if [ ! -f "$assembly_info" ]; then
              assembly_info=$(find "$plugin_dir" -name AssemblyInfo.cs | head -n 1)
            fi
            if [ -z "$assembly_info" ] || [ ! -f "$assembly_info" ]; then
              echo "AssemblyInfo.cs not found for $deb_package" >&2
              exit 1
            fi
            assembly_version_line=$(grep -E 'AssemblyVersion' "$assembly_info" || true)
            plugin_version=$(echo "$assembly_version_line" | sed -E 's/.*"([^\"]+)".*/\1/')
            if [ -z "$plugin_version" ]; then
              echo "AssemblyVersion not found in $assembly_info" >&2
              exit 1
            fi

            # Ensure every CI build produces a unique, increasing Debian version.
            plugin_version="${plugin_version}+${GITHUB_RUN_NUMBER}"
            rm -rf "$tmp_dir" "$plugin_dir/app"
            git clone --branch pins --single-branch https://github.com/Touch-N-Stars/Touch-N-Stars.git "$tmp_dir"
            pushd "$tmp_dir" >/dev/null
            npm install
            npm run build
            popd >/dev/null
            mv "$tmp_dir/dist" "$plugin_dir/app"
            dotnet restore "$project_path" -r ${{ env.TARGET_RUNTIME }}
            dotnet build "$project_path" -c ${{ env.BUILD_CONFIGURATION }} -f ${{ env.PLUGIN_TARGET_FRAMEWORK }} -r ${{ env.TARGET_RUNTIME }} --no-restore ${{ env.PLUGIN_MSBUILD_ARGS }}
            tfm_dir="$plugin_dir/bin/${{ env.BUILD_CONFIGURATION }}/${{ env.PLUGIN_TARGET_FRAMEWORK }}/${{ env.TARGET_RUNTIME }}"
            if [ ! -d "$tfm_dir" ]; then
              echo "Expected RID output folder not found at: $tfm_dir" >&2
              echo "Falling back to first 'net*' folder under bin/${{ env.BUILD_CONFIGURATION }}" >&2
              build_root="$plugin_dir/bin/${{ env.BUILD_CONFIGURATION }}"
              tfm_dir=$(find "$build_root" -maxdepth 1 -type d -name 'net*' | head -n 1)
              if [ -z "$tfm_dir" ]; then
                echo "Could not locate target framework output under $build_root" >&2
                exit 1
              fi
            fi
            deb_root="${{ env.PLUGIN_DEB_BASE }}/$deb_package"
            install_root="$deb_root${{ env.PLUGINS_INSTALL_BASE }}/$install_folder"
            rm -rf "$deb_root"
            mkdir -p "$install_root"
            # Exclude core NINA files from plugin package
            core_publish_dir="${{ env.PUBLISH_DIRECTORY }}/${{ env.TARGET_RUNTIME }}"
            exclude_list="$(pwd)/core_excludes.txt"
            (cd "$core_publish_dir" && find . -type f -printf "%P\n") > "$exclude_list"
            rsync -a --exclude-from="$exclude_list" "$tfm_dir/" "$install_root/"
            rm -f "$exclude_list"
            find "$install_root" -mindepth 1 -maxdepth 1 -type d -name "??-??" -exec rm -rf {} +
            find "$install_root" -mindepth 1 -maxdepth 1 -type d -name "??" -exec rm -rf {} +
            app_dir="$plugin_dir/app"
            if [ -d "$app_dir" ]; then
              rm -rf "$install_root/app"
              cp -a "$app_dir" "$install_root/app"
            fi
            extra_libs="$plugin_dir/extra-libs"
            if [ -d "$extra_libs" ]; then
              find "$extra_libs" -maxdepth 1 -type f -name '*.dll' -exec cp {} "$install_root/" \;
            fi
            control_dir="$deb_root/DEBIAN"
            mkdir -p "$control_dir"
            installed_size=$(du -sk "$deb_root${{ env.PLUGINS_INSTALL_BASE }}" | cut -f1)
            {
              echo "Package: $deb_package"
              echo "Version: $plugin_version"
              echo "Section: misc"
              echo "Priority: optional"
              echo "Architecture: ${{ env.DEB_ARCH }}"
              echo "Maintainer: PINS Team"
              echo "Installed-Size: $installed_size"
              echo "Description: NINA plugin package for $install_folder"
            } > "$control_dir/control"
            {
              echo "#!/bin/sh"
              echo "set -e"
              echo "chown -R pi:pi ${{ env.PLUGINS_INSTALL_BASE }}/$install_folder || true"
              echo "exit 0"
            } > "$control_dir/postinst"
            chmod 755 "$control_dir/postinst"
            deb_path="${{ env.PLUGIN_ARTIFACT_DIR }}/${deb_package}_${plugin_version}_${{ env.DEB_ARCH }}_${{ steps.release_tag.outputs.date }}.deb"
            dpkg-deb --root-owner-group --build "$deb_root" "$deb_path"
            sha256sum "$deb_path" > "$deb_path.sha256"
          )
          rc=$?
          set -e
          if [ $rc -eq 0 ]; then
            echo "Built $deb_package"
          else
            echo "$deb_package" | tee -a "${{ env.PLUGIN_FAILURES_FILE }}"
          fi

      - name: Build Pins Plugin deb
        shell: bash
        run: |
          set -euo pipefail
          project_path="NINA.Plugins/pins.plugin/PI'N'Stars.csproj"
          install_folder="pins.plugin"
          deb_package="pins-plugin-pins"
          plugin_dir=$(dirname "$project_path")
          set +e
          (
            set -euo pipefail

            # Build pins.powerbox.sdk
            echo "Building pins.powerbox.sdk..."
            powerbox_sdk_tmp="$(mktemp -d)"
            git clone --recursive https://github.com/nitr57/pins.powerbox.sdk "$powerbox_sdk_tmp"
            pushd "$powerbox_sdk_tmp" >/dev/null
            mkdir -p build && cd build
            cmake ..
            make
            mkdir -p "$plugin_dir/extra-libs"
            echo "Copying libpowerboxsdk.so to $plugin_dir/extra-libs/PowerBoxSDK.dll"
            cp libpowerboxsdk.so "$plugin_dir/extra-libs/PowerBoxSDK.dll"
            popd >/dev/null
            rm -rf "$powerbox_sdk_tmp"

            # Build pins.meteostation.sdk
            echo "Building pins.meteostation.sdk..."
            meteostation_sdk_tmp="$(mktemp -d)"
            git clone --recursive https://github.com/nitr57/pins.meteostation.sdk "$meteostation_sdk_tmp"
            pushd "$meteostation_sdk_tmp" >/dev/null
            mkdir -p build && cd build
            cmake ..
            make
            mkdir -p "$plugin_dir/extra-libs"
            echo "Copying libmeteostationsdk.so to $plugin_dir/extra-libs/MeteostationSDK.dll"
            cp libmeteostationsdk.so "$plugin_dir/extra-libs/MeteostationSDK.dll"
            popd >/dev/null
            rm -rf "$meteostation_sdk_tmp"

            assembly_info="$plugin_dir/Properties/AssemblyInfo.cs"
            if [ ! -f "$assembly_info" ]; then
              assembly_info=$(find "$plugin_dir" -name AssemblyInfo.cs | head -n 1)
            fi
            if [ -z "$assembly_info" ] || [ ! -f "$assembly_info" ]; then
              echo "AssemblyInfo.cs not found for $deb_package" >&2
              exit 1
            fi
            
            # Try robust grep first (for active attribute)
            assembly_version_line=$(grep -E '^\s*\[assembly:\s*AssemblyVersion' "$assembly_info" || true)
            # Fallback to loose grep if robust found nothing (e.g. commented out)
            if [ -z "$assembly_version_line" ]; then
                assembly_version_line=$(grep -E 'AssemblyVersion' "$assembly_info" || true)
            fi

            plugin_version=$(echo "$assembly_version_line" | sed -E 's/.*"([^\"]+)".*/\1/')
            if [ -z "$plugin_version" ]; then
              echo "AssemblyVersion not found in $assembly_info" >&2
              exit 1
            fi

            # Ensure every CI build produces a unique, increasing Debian version.
            plugin_version="${plugin_version}+${GITHUB_RUN_NUMBER}"
            dotnet restore "$project_path" -r ${{ env.TARGET_RUNTIME }}
            dotnet build "$project_path" -c ${{ env.BUILD_CONFIGURATION }} -f ${{ env.PLUGIN_TARGET_FRAMEWORK }} -r ${{ env.TARGET_RUNTIME }} --no-restore ${{ env.PLUGIN_MSBUILD_ARGS }}
            tfm_dir="$plugin_dir/bin/${{ env.BUILD_CONFIGURATION }}/${{ env.PLUGIN_TARGET_FRAMEWORK }}/${{ env.TARGET_RUNTIME }}"
            if [ ! -d "$tfm_dir" ]; then
              echo "Expected RID output folder not found at: $tfm_dir" >&2
              echo "Falling back to first 'net*' folder under bin/${{ env.BUILD_CONFIGURATION }}" >&2
              build_root="$plugin_dir/bin/${{ env.BUILD_CONFIGURATION }}"
              tfm_dir=$(find "$build_root" -maxdepth 1 -type d -name 'net*' | head -n 1)
              if [ -z "$tfm_dir" ]; then
                echo "Could not locate target framework output under $build_root" >&2
                exit 1
              fi
            fi
            deb_root="${{ env.PLUGIN_DEB_BASE }}/$deb_package"
            install_root="$deb_root${{ env.PLUGINS_INSTALL_BASE }}/$install_folder"
            rm -rf "$deb_root"
            mkdir -p "$install_root"
            # Exclude core NINA files from plugin package
            core_publish_dir="${{ env.PUBLISH_DIRECTORY }}/${{ env.TARGET_RUNTIME }}"
            exclude_list="$(pwd)/core_excludes.txt"
            (cd "$core_publish_dir" && find . -type f -printf "%P\n") > "$exclude_list"
            rsync -a --exclude-from="$exclude_list" "$tfm_dir/" "$install_root/"
            rm -f "$exclude_list"
            find "$install_root" -mindepth 1 -maxdepth 1 -type d -name "??-??" -exec rm -rf {} +
            find "$install_root" -mindepth 1 -maxdepth 1 -type d -name "??" -exec rm -rf {} +
            extra_libs="$plugin_dir/extra-libs"
            if [ -d "$extra_libs" ]; then
              find "$extra_libs" -maxdepth 1 -type f -name '*.dll' -exec cp {} "$install_root/" \;
            fi
            app_dir="$plugin_dir/app"
            if [ -d "$app_dir" ]; then
              rm -rf "$install_root/app"
              cp -a "$app_dir" "$install_root/app"
            fi
            control_dir="$deb_root/DEBIAN"
            mkdir -p "$control_dir"
            installed_size=$(du -sk "$deb_root${{ env.PLUGINS_INSTALL_BASE }}" | cut -f1)
            {
              echo "Package: $deb_package"
              echo "Version: $plugin_version"
              echo "Section: misc"
              echo "Priority: optional"
              echo "Architecture: ${{ env.DEB_ARCH }}"
              echo "Maintainer: PINS Team"
              echo "Installed-Size: $installed_size"
              echo "Description: NINA plugin package for $install_folder"
            } > "$control_dir/control"
            {
              echo "#!/bin/sh"
              echo "set -e"
              echo "chown -R pi:pi ${{ env.PLUGINS_INSTALL_BASE }}/$install_folder || true"
              echo "exit 0"
            } > "$control_dir/postinst"
            chmod 755 "$control_dir/postinst"
            deb_path="${{ env.PLUGIN_ARTIFACT_DIR }}/${deb_package}_${plugin_version}_${{ env.DEB_ARCH }}_${{ steps.release_tag.outputs.date }}.deb"
            dpkg-deb --root-owner-group --build "$deb_root" "$deb_path"
            sha256sum "$deb_path" > "$deb_path.sha256"
          )
          rc=$?
          set -e
          if [ $rc -eq 0 ]; then
            echo "Built $deb_package"
          else
            echo "$deb_package" | tee -a "${{ env.PLUGIN_FAILURES_FILE }}"
          fi

      - name: Summarize plugin build status
        if: always()
        run: |
          if [ -s "${{ env.PLUGIN_FAILURES_FILE }}" ]; then
            echo "The following plugin packages failed to build:" >&2
            cat "${{ env.PLUGIN_FAILURES_FILE }}" >&2
            exit 1
          fi
          echo "All plugin packages built successfully."

      - name: Publish deb-only GitHub release
        uses: softprops/action-gh-release@v2
        if: success()
        with:
          tag_name: ${{ steps.release_tag.outputs.value }}
          name: Release ${{ steps.release_tag.outputs.value }}
          body: |
            Automated release ${{ steps.release_tag.outputs.value }} containing the Raspberry Pi build and plugin Debian packages.
          files: |
            artifacts/${{ env.DEB_PACKAGE }}_${{ steps.version.outputs.package_version }}_${{ env.DEB_ARCH }}_${{ steps.release_tag.outputs.date }}.deb
            artifacts/pinsdaemon_*.deb
            artifacts/plugins/*.deb
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload plugin deb artifacts
        uses: actions/upload-artifact@v4
        with:
          name: plugin-debs
          path: ${{ env.PLUGIN_ARTIFACT_DIR }}
          retention-days: 30